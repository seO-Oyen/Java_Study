# 객체 대 객체 관계 내용 계열화

1. 객체 대 객체 관계의 프로그램적인 이해
	- 실무적인 상황
		로그인한 회원이 단일 물건 구매
		MVC 패턴에서 Constroller가 Service단 호출
			Service단이 DAO단 호출
			인사 프로그램에서 사원이 특정한 부서에 소속된 처리
2. 단계별 프로그램 처리
	1) 필드 선언
		- 포함될 클래스 선언
		- 포함할 클래스 선언하면 필드값으로 선언
	2) 생성자 또는 메서드의 매개변수로 객체 전달
		void buyCar(Car car) {
			this.car = car;
		}
		
	3) 전달된 객체의 메모리에 따른 기능 함수의 처리
		void drivingCar() {
			if (car != null) {
			} else{
			}
		}
		