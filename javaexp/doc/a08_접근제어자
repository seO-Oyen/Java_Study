# 접근제어자

1. 프로그램에서 클래스 단위로 구성요소 (멤버 - 필드 / 메서드)를 외부 클래스에서 호출해서 사용하는 경우가 많다.
2. 외부 클래스들이 해당 클래스의 클래스 / 멤버에 대한 접근을 범위에 따라서 제한을 두어 보다 효과적으로 데이터를 보호하면
   적절한 범위로 데이터를 전달하려는 목적으로 사용한다.
3. 사전개념
	1) 객체 vs 객체 호출 개념
	2) 클래스내에서만 사용하는 데이터 : 필드 (핵심 - 읽고/쓰기) -> private
	3) 패키지 : 묶음, 클래스를 기능별로 범위로 묶어서 처리할 때 사용
	4) 상속 : 클래스와 클래스간의 상속처리
	5) 모두 다 접근 가능 : import, 패키지명.클래스명
4. 단계별 실습
	1) private vs default
	2) default vs public
	3) default vs protected
	4) protected vs public
	
# protected 접근제어자
1. 접근범위 default < protected < public